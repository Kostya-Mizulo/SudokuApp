import 'package:flutter/material.dart';

class UserMenuBody extends StatelessWidget {
  const UserMenuBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
